import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'optimanager2.0.1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
